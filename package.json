{
  "name": "shadycut-website",
  "private": true,
  "scripts": {
    "components": "node scripts/inline-components.js",
    "social": "node scripts/generate-social-images.js",
    "routes": "node scripts/generate-routes.js",
    "sitemap": "node scripts/generate-sitemap.js",
    "build": "npm run components && npm run social && npm run routes && npm run sitemap",
    "test": "node --test \"tests/**/*.test.js\""
  }
}
